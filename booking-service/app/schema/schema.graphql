type Booking {
  id: ID!
  vehicleId: Int!
  bookingDate: String!
  status: String!
}

type Query {
  myBookings: [Booking!]
}

type Mutation {
  createBooking(vehicleId: Int!, date: String!): Booking
}
